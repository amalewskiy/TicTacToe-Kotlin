<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tictactoe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TicTacToe">
        <activity
            android:name=".play_online.boardOnline"
            android:label="@string/title_activity_board_online"
            android:theme="@style/Theme.TicTacToe.NoActionBar"></activity>
        <activity android:name=".play_with_friend.Friend" />
        <activity android:name=".friends.Friends" />
        <activity android:name=".play_with_friend.friend" />
        <activity android:name=".Android" />
        <activity android:name=".play_online.Online" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>